import { PaginationType } from '../types/Pagination';

const dataCenter: PaginationType = {
    currentPage: 15,
    lastPage: 30,
};

const dataStart: PaginationType = {
    currentPage: 1,
    lastPage: 50,
};

const dataEnd: PaginationType = {
    currentPage: 40,
    lastPage: 40,
};

const dataShort: PaginationType = {
    currentPage: 1,
    lastPage: 1,
};

export {
    dataCenter,
    dataStart,
    dataEnd,
    dataShort,
};
