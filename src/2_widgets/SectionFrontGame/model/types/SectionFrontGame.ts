import { PictureType } from '5_shared/types/statamic';

interface SectionFrontGameType {
    title: string;
    content: string;
    picture: PictureType;
}

export type {
    SectionFrontGameType,
};
