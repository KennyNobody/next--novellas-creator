import { PictureType } from '5_shared/types/statamic';

interface SectionFrontAboutType {
    title: string;
    content: string;
    picture: PictureType;
}

export type {
    SectionFrontAboutType,
};
