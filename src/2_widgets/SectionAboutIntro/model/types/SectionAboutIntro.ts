import { PictureType } from '4_entities/Picture';

interface SectionAboutIntroType {
    title: string;
    content: string;
    background: PictureType
}

export {
    type SectionAboutIntroType,
};
